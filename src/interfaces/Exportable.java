package interfaces;

public interface Exportable {
    /* 
     * @brief Exporta los datos de los productos a un archivo de texto en la ruta especificada.
     * @param path La ruta del archivo de texto donde se exportarán los datos.
    */
    void exportarTXT(String path);
}
